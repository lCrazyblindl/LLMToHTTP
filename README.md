# LLMToHTTP
LLM TO HTTP Improvement

Sandbox repo for experimenting with exposing tools over HTTP, including MCP.

## Projects

- [`pet-zoo/`](pet-zoo/README.md) — a small FastAPI zoo management API (CRUD for monkeys, lions, tigers, elephants), JSON file storage, Docker support, Swagger docs. Built first as a plain web server; the target for upcoming MCP experiments.
